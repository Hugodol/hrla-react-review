import React, { Component } from 'react';
import { Switch, Route } from 'react-router-dom';
// TODO import needed components

// TODO create statefull Routes component
  // state: myMemes = array

  // TODO create method to set new meme to state

  /* jsx to render
    <div>
      <Switch>
        <Route
          exact
          path="/"
          render={() => (
            // TODO render Generator component
        )}
        />
        <Route
          path="/mymemes"
          render={() => (
            // TODO render MyMemes component
        )}
        />
      </Switch>
    </div>
  */
